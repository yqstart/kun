# 更新日志

本文件为发布正文提供内容（`scripts/release-notes.sh` 提取最新版本段落）。

## [0.5.0] - 2026-08-14

### 新功能

- **无边框窗口整体圆角**（macOS）：关闭系统标题栏后窗口四角圆角（AppKit backing layer `cornerRadius=12`），圆角外侧透明露出桌面，与 Terminal.app 观感一致
- **窗口拖拽**：无系统标题栏后，标签栏空白处按下拖动即可移动窗口（底层 drag 背景 + `StartDrag`，控件点击不受影响）
- **自绘 macOS traffic lights**：标签栏最左红/黄/绿三圆点（关闭/最小化/全屏），与标签栏同处一行
- **设置弹窗**（`⌘,` / 齿轮 ⚙）：卡片化三组——主机管理（新建连接/删除/双击连接）、外观（三套深色主题切换）、关于（版本号 + 检查更新）；替代原左侧主机栏与顶部工具栏入口
- **SFTP 面板 tabby 化**：默认收起只显示终端，终端右上角悬浮「SFTP」按钮展开/收起（滑动动画 + 右缘拖拽把手），最大宽度钳制约窗口 45% 防止挤压终端
- **标签页数字索引**：每个标签前显示 `1, 2, …` 序号

### 界面

- **品牌 logo 纯 K 字**：移除 ikun 形象与篮球元素（`ikun_face.png` / `extract-ikun.py` 一并删除），改为紫金渐变圆底 + 白色粗体 K 字居中，与应用图标同构图；移除持续重绘动画（省电）
- 标签栏最右侧精简为单个设置齿轮（移除复制 tab 按钮）

### 修复

- **终端删除/回车问题根治**：
  - 本地会话注入 `TERM=xterm-256color`（GUI/Dock 启动继承 `TERM=dumb` 导致 zsh 判定非交互终端，删除键回显「原地空格覆盖」、回车异常）
  - 主键盘 Enter 永远发送 `\r`（曾受应用键盘模式影响误编码为 `ESC O M`，zsh 启用应用键盘模式后回车不执行）
- **scrollback 滚动渲染闪退**：`display_iter` 负网格行号 cast 成 `usize::MAX` 触发 capacity overflow（向上滚动查看历史后崩溃）
- **补全浮层遮挡输入行**：定位改为以光标行底部为基准，上方空间充足时浮层完全不遮输入行
- **SFTP 文件列表列对齐/截断**：列宽改按 `'0'` 字符宽计算（时间列不再被截断成 "2026-01"），长文件名不再挤歪大小/时间列，整行可点击

### 依赖

- 新增 macOS 原生依赖：`objc2-app-kit`（NSWindow/NSView/CALayer）、`objc2`、`raw-window-handle`

## [0.4.0] - 2026-08-13

### 新功能

- **终端命令/文件补全**（Warp 风格，仅本地会话）：输入命令前缀弹出候选浮层——命令位置匹配 PATH 可执行文件，其余匹配当前目录文件/目录（目录带 `/` 后缀）；Tab 确认补全、↑/↓ 选择候选、Esc 关闭；随输入实时重算（最多 8 条）
- **终端 `ls` 颜色**：注入 `CLICOLOR=1` 与深色优化 `LSCOLORS`（目录亮青/链接紫红/可执行红），与 Terminal.app/iTerm2 行为一致

### 改进

- ikun logo 支持自定义纹理：`assets/ikun_face.png` 抠图作为扩展点（未来可在 K 字位置直接贴图替换）
- 更新弹窗发布说明保留段落/列表结构（不再挤成一大段文字）

### 修复

- **新建连接表单输入框文字顶边**：egui 0.36 自定义 frame 时 `TextEdit::margin()` 被丢弃（默认内边距为 0）——内边距改挂到 frame 的 `inner_margin`，文字恢复居中留白

## [0.3.0] - 2026-08-13

### 界面

- **品牌 logo 重设计**：ikun 篮球轨迹 + K 字 + 紫金霓虹光环（hover 辉光），工具栏品牌区整体可点击折叠主机列表
- **新建连接对话框重设计**：居中 logo + 标题引导，表单分组（连接身份 / 网络地址 / 认证方式），字段定宽统一
- 主机行选中增加 accent 描边，选中状态更醒目
- 主题精简为三套深色（深色 / 深蓝 / 霓虹，移除 Dawn 浅色），快捷键改为 ⌥1-⌥3
- 移除工具栏底部扫光动画（省电）

### 修复

- **修复 SFTP 连接竞态**：SSH 标签页创建前 SFTP 已就绪时会话丢失（新增 `ready_sftp` 挂载队列，标签创建后自动挂载）
- **端口输入校验**：仅接受 1-65535 数字，非法输入 toast 提示并中止连接
- 退格空白文本抑制由单帧扩展为 2 帧（覆盖输入法延迟发送的空格类事件）

## [0.2.0] - 2026-08-13

### 界面重设计

- **Warp 风格视觉体系**：分层深色背景（#0E0E11/#151519/#1A1A20/#24242C）、紫青品牌渐变、圆角幽灵按钮、菜单/弹窗阴影与圆角
- **动效**：工具栏底部循环扫光（紫→青）、渐变 logo hover 辉光、标签页选中底部指示条动画、主机行 hover/选中平滑过渡、Toast 滑入淡出、更新状态呼吸灯
- **四套主题刷新**：默认主题更名为「Warp 深色」，Dawn/Midnight/Cyberpunk 同步细化配色与终端调色板
- 标签栏 Warp 风格圆角标签 + 选中指示条；状态栏带运行状态圆点与快捷键提示；空状态居中 logo 引导页

### 应用内更新

- 版本检查改走 `releases.atom`（不受 GitHub API 未认证限流影响）
- **应用内一键更新**：发现新版后直接下载对应架构 dmg（进度条 + 扫光），挂载、替换安装到「应用程序」并自动重启，无需跳转网站
- 安装失败自动回退到 `~/Applications`；下载错误可在弹窗内重试

### 安装体验

- dmg 内新增 **Applications 快捷方式**（与常规 macOS 应用一致：拖拽即安装），本机打包时自动美化磁盘镜像图标布局
- .app 附加 ad-hoc 签名，Info.plist 补充 `NSSupportsAutomaticGraphicsSwitching`

## [0.1.1] - 2026-08-12

### 新增

- **多标签页**（warp 风格）：标签栏新建（＋/⌘T）、切换（点击/⌘1-9）、关闭（×/⌘W），SFTP 按标签页挂载
- **左侧主机列表可折叠**：工具栏 ◧/⌘B 切换，默认收起直接进入终端
- **主机条目新样式**：accent 圆形头像（主机名首字符）+ 垃圾桶删除图标，双击条目连接
- **运行时应用图标**：Dock 显示渐变紫 K 图标（无需 .app bundle）

### 优化

- 工具栏精简：移除「kun」标题与「新建连接」「本地终端」按钮（入口移至侧栏/标签栏）
- 终端内容内边距（对齐 Terminal.app 观感）
- 本地终端默认工作目录为 home（Finder 启动不再落在根目录）

### 修复

- 修复 zsh 提示符符号（➜/❯/⚡ 等）缺字形显示为 `?`（Menlo 等宽符号 fallback）
- 修复删除键插入空格（微信输入法等输入法退格时伴随空格文本，已过滤）
- 修复终端背景与点击区域失效（egui `min_rect` 0x0 陷阱）
- 修复浅色主题下终端背景被 OSC 11 覆盖

## [0.1.0] - 2026-08-12

### 新增

- **本地终端**：基于 alacritty_terminal 生产级内核，支持 256 色、宽字符、滚动、括号粘贴、闪烁光标
- **SSH 远程终端**：密码 / 私钥认证（支持口令），xterm-256color PTY
- **SFTP 可视化**：双栏分屏（终端 | SFTP 可拖拽），目录导航、上传/下载带进度条、删除/重命名/新建目录（带确认对话框）
- **主机管理**：主机配置持久化（`~/.config/kun/hosts.toml`），单击选中、双击连接

### 样式

- **四套主题**（对齐 MiroCode）：Miro 深色（紫）/ Dawn 浅色 / Midnight 深蓝 / Cyberpunk 霓虹，每套含独立终端调色板
- 工具栏主题下拉切换（⌥1-⌥4 快捷键）
- 终端调色板：Catppuccin Mocha 16 色 + xterm 256 色表
- 浅色主题兼容：终端背景跟随主题、macOS 标题栏同步系统主题

### 修复

- 修复点击"新建连接"后崩溃（ui.input 闭包内 request_repaint 死锁）
- 修复输入法产生的零宽字符插入终端（退格键异常）
- 修复中文显示豆腐块（CJK fallback 字体）

### 安装

- **macOS**：下载 `kun-<版本>-macos.dmg`，打开并拖入"应用程序"；首次打开若提示未认证，右键 → 打开
> 注：当前版本未签名/未公证，macOS 首次打开需在"系统设置 → 隐私与安全性"中允许。
